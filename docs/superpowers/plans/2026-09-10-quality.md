# План 3. Качество

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** превратить обещания спецификации в машинную проверку: golden-корпус в файлах,
сверка с оракулом Лебедева, прогон каждого правила поодиночке, фаззинг и работающий CI.

**Architecture:** ни строчки в `src/` — план целиком про `tests/`, `fuzz/` и `.github/`.
Четыре независимых уровня проверки: файловый корпус (читаемые эталоны), реестровый прогон
(каждое из 107 правил в одиночку), снимок оракула (наши расхождения с Лебедевым закреплены
таблицей) и фаззинг (libFuzzer по расписанию). Сеть тестами CI не трогается: живой оракул
живёт отдельным проектом и пропускает себя, пока не задана переменная окружения.

**Tech Stack:** C# 13, .NET 10 SDK, xunit.v3 на Microsoft.Testing.Platform, SharpFuzz +
libFuzzer, BenchmarkDotNet, GitHub Actions.

**Spec:** `docs/spec.md`, раздел 9 «Тестирование» и раздел 8 «Гарантии».

## Global Constraints

- Целевые платформы ядра: `netstandard2.0;net8.0;net10.0`. Тестовые и вспомогательные
  проекты — только `net10.0`.
- `TreatWarningsAsErrors` включён на весь репозиторий (`Directory.Build.props`).
- `dotnet test` запускается БЕЗ префикса `rtk` — под Microsoft.Testing.Platform фильтр rtk
  печатает «0 tests, exit code 5», хотя тесты проходят. Все остальные команды — с `rtk`.
- Ветка по умолчанию — `master`, интеграционная — `develop`. Теги версий без `v`.
- Комментарии, имена тестов и документация — по-русски, как во всём репозитории.
- Сеть в тестах, которые запускает CI, запрещена.
- Реестр правил закрыт: 107 правил, из них не реализованы `common/punctuation/quoteLink`,
  `common/html/stripTags`, `common/html/processingAttrs`, отложено
  `ru/typo/switchingKeyboardLayout`.

---

## Файловая структура

| Файл | Ответственность |
|---|---|
| `tests/Typographer.Corpus/*.in.txt` | входы golden-корпуса, по теме на файл |
| `tests/Typographer.Corpus/*.out.txt` | эталонные выходы, генерируются и проверяются глазами |
| `tests/Typographer.Tests/Corpus/CorpusFiles.cs` | поиск каталога корпуса и чтение пар |
| `tests/Typographer.Tests/Corpus/CorpusFileTests.cs` | сверка с эталонами и режим перегенерации |
| `tests/Typographer.Tests/Rules/EveryRuleTests.cs` | прогон каждого правила реестра в одиночку |
| `tests/Typographer.Tests/Oracle/OracleSnapshot.cs` | разбор таблицы `docs/oracle/lebedev.md` |
| `tests/Typographer.Tests/Oracle/OracleSnapshotTests.cs` | наши расхождения с оракулом закреплены |
| `tests/Typographer.Oracle/` | живая сверка с веб-сервисом, вне CI |
| `fuzz/Typographer.Fuzz/` | цель фаззинга для libFuzzer |
| `.github/workflows/fuzz.yml` | фаззинг по расписанию |
| `docs/perf.md` | замер производительности после того, как Bind стал писать документ |

Корпус лежит НЕ внутри тестового проекта: спецификация, раздел 10, называет его отдельным
каталогом `tests/Typographer.Corpus/`, и смысл в этом есть — файлы читает человек, а не
компилятор, и в каталоге не должно быть ничего, кроме входов и выходов.

---

## Task 1: Golden-корпус в файлах

**Files:**
- Create: `tests/Typographer.Corpus/*.in.txt` (десять файлов)
- Create: `tests/Typographer.Corpus/*.out.txt` (десять файлов, генерируются шагом 4)
- Create: `tests/Typographer.Tests/Corpus/CorpusFiles.cs`
- Create: `tests/Typographer.Tests/Corpus/CorpusFileTests.cs`
- Modify: `tests/Typographer.Tests/Typographer.Tests.csproj`
- Create: `.gitattributes`

**Interfaces:**
- Produces: `CorpusFiles.Directory` (путь к каталогу корпуса),
  `CorpusFiles.Names` (`TheoryData<string>` с именами без расширения),
  `CorpusFiles.ReadInput(string name)`, `CorpusFiles.ReadExpected(string name)` —
  всё используется задачей 2 как источник входных данных и задачей 5 как затравка фаззера.

- [ ] **Step 1: Передать путь к корпусу в сборку тестов**

Тест обязан знать, где лежит каталог корпуса, и путь этот нужен ИСХОДНЫЙ, а не в `bin`:
режим перегенерации перезаписывает эталоны, и переписывать их в каталоге сборки бессмысленно.
Вычислять путь прыжками `../../../..` от `AppContext.BaseDirectory` нельзя — число прыжков
зависит от конфигурации и целевой платформы. Правильный способ — атрибут сборки, значение
которому подставляет MSBuild.

Добавить в `tests/Typographer.Tests/Typographer.Tests.csproj` перед закрывающим `</Project>`:

```xml
  <ItemGroup>
    <AssemblyAttribute Include="System.Reflection.AssemblyMetadataAttribute">
      <_Parameter1>CorpusDirectory</_Parameter1>
      <_Parameter2>$(MSBuildThisFileDirectory)../Typographer.Corpus</_Parameter2>
    </AssemblyAttribute>
  </ItemGroup>
```

- [ ] **Step 2: Написать чтение корпуса**

Создать `tests/Typographer.Tests/Corpus/CorpusFiles.cs`:

```csharp
using System.Reflection;

namespace Typographer.Tests.Corpus;

/// <summary>Golden-корпус: пары файлов «вход — эталонный выход» в tests/Typographer.Corpus.</summary>
/// <remarks>
/// Каталог найден через атрибут сборки, а не прыжками вверх от каталога сборки: число
/// прыжков зависит от конфигурации и целевой платформы, а путь нужен ИСХОДНЫЙ — режим
/// перегенерации перезаписывает эталоны в репозитории, а не в bin.
/// Перевод строки при чтении приводится к «\n», а последний отбрасывается: файл в git
/// заканчивается переводом строки по соглашению, и этот перевод — свойство файла, а не
/// входных данных.
/// </remarks>
public static class CorpusFiles
{
    /// <summary>Каталог корпуса. Абсолютный путь, полученный из пути к csproj во время сборки.</summary>
    public static string Directory { get; } = Path.GetFullPath(
        typeof(CorpusFiles).Assembly
            .GetCustomAttributes<AssemblyMetadataAttribute>()
            .Single(a => a.Key == "CorpusDirectory")
            .Value!);

    /// <summary>Имена случаев корпуса без расширений, в алфавитном порядке.</summary>
    public static TheoryData<string> Names
    {
        get
        {
            var data = new TheoryData<string>();
            foreach (string name in EnumerateNames())
            {
                data.Add(name);
            }

            return data;
        }
    }

    /// <summary>Имена случаев корпуса — то же самое, но пригодное для обхода в цикле.</summary>
    public static IEnumerable<string> EnumerateNames()
        => System.IO.Directory.EnumerateFiles(Directory, "*.in.txt")
            .Select(path => Path.GetFileName(path)[..^".in.txt".Length])
            .OrderBy(name => name, StringComparer.Ordinal);

    /// <summary>Читает вход случая.</summary>
    public static string ReadInput(string name) => Read($"{name}.in.txt");

    /// <summary>Читает эталонный выход случая. Пустая строка, если эталона ещё нет.</summary>
    public static string ReadExpected(string name)
    {
        string path = Path.Combine(Directory, $"{name}.out.txt");
        return File.Exists(path) ? Read($"{name}.out.txt") : string.Empty;
    }

    /// <summary>Записывает эталонный выход случая. Только режим перегенерации.</summary>
    public static void WriteExpected(string name, string value)
        => File.WriteAllText(Path.Combine(Directory, $"{name}.out.txt"), value + "\n");

    private static string Read(string fileName)
    {
        string text = File.ReadAllText(Path.Combine(Directory, fileName)).Replace("\r\n", "\n");
        return text.EndsWith('\n') ? text[..^1] : text;
    }
}
```

- [ ] **Step 3: Написать входы корпуса**

Создать десять файлов в `tests/Typographer.Corpus/`. Темы взяты из таблицы сложных случаев
спецификации, раздел 9; одиночные суррогаты в корпус не попадают — их в файле UTF-8 не
записать, и они остаются в `HardCases`, где строятся из кода.

`quotes.in.txt`:

```
Эксперт уточнил: "в перечень включен закон "О стандартизации"".
Диагональ 15,6" при росте 5'6" и угле 30°15'20".
Кавычки вокруг ссылки: "<a href="/a">Ссылка</a>" и <b>сло</b>во.
Он сказал,"да" и на этом всё.
"Да," сказал он. "Второй"
```

`dashes.in.txt`:

```
Тире - вот так, а дефис-минус - нет.
- Привет, - сказал он тихо.
Он вошёл в комнату.
- Пойдём, - сказал он.
Годы 1941-1945 и температура от -5 до +5 градусов.
Из-за угла по-русски, кое-что из-под стола.
```

`numbers.in.txt`:

```
Число 3.14 против версии 1.2.3 и адреса 192.168.0.1 и даты 01.01.2020.
Формула 3 x 4 = 12, дробь 1/2, неравенство 8 != 9 и 5 <= 6.
Разряды 1000000 рублей и 25-ый дом в 2026году.
Проценты 50 % и 100%, градусы 25 C.
```

`abbreviations.in.txt`:

```
А.С. Пушкин писал в 3 г. IV в. до н. э.
Адрес: ул. Ленина, д. 5, кв. 7, стр. 15, см. рис. 3.
Скорость 10 км/ч, объём 3 л, вес 10 кг.
ООО Ромашка купило 10 млн на 5 тыс. руб. в 2012 г.
Номер № 5 и параграф § 3 и пункт ¶ 3.
```

`punctuation.in.txt`:

```
Вот?.. и вот!.. и смайл :-) в конце.
Многоточие... и ещё .... и текст. ..
Раз,,два и три,.четыре и пять,;шесть.
Что!? Ура!... Текст?..Как
Слово(текст) и [ ещё ] и текст:ещё и раз;два.
```

`html-traps.in.txt`:

```
<code>a - b</code> и <a title="Не трогать - тире">x</a>.
<!--[if IE]>только тут<![endif]--> и <![CDATA[ x > a - b ]]> конец.
<pre><!-- </pre> -->a - b</pre>
<pre><a title="</pre>">x</a> a - b</pre>
<code>outer <code>inner</code> a - b</code>
<code-block>a - b</code-block> "а" - б
```

`markup-context.in.txt`:

```
<p>Первый.</p><p>"Цитата"</p><p>- Привет</p>
Привет<br />- Пока
$<b>100</b> и 5<sup>2</sup> м.
<b>А</b>- б и <b>сло</b>во.
<nobr>в доме</nobr> и в доме.
```

`links.in.txt`:

```
Сайт http://example.com/a-b?x=1&y=2 и почта user@example.com.
Смотри на www.example.com - там всё есть.
```

`mixed.in.txt`:

```
Windows 10 - "лучшая" ОС.
д'Артаньян, O'Neil и Кот-д'Ивуар.
Дата 2026-09-10, 9 Сентября 2026, 2 мая, понедельник.
Телефон +7 (999) 123-45-67 и артикул 89991234567890.
```

`article.in.txt` — связный текст, а не набор ловушек: корпус должен показывать, как
типограф ведёт себя на обычном материале.

```
<h2>О типографике</h2>
<p>Типографика - это не украшение. Это способ показать читателю структуру текста
до того, как он начнёт читать. "Хорошая типографика незаметна", - говорил Ян
Чихольд, и был прав: заметна только плохая.</p>
<p>Правил немного. Кавычки в русском тексте - ёлочки, а вложенные - лапки.
Тире отделяется пробелами, дефис - нет. Между числом и единицей измерения -
неразрывный пробел: 10 кг, 5 м, 100 %. Инициалы не отрываются от фамилии:
А. С. Пушкин.</p>
<p>Остальное - вкус и мера. Как писал В. А. Фаворский, "искусство книги не в
том, чтобы книга была красива, а в том, чтобы она была книгой".</p>
```

- [ ] **Step 4: Написать тест корпуса и сгенерировать эталоны**

Создать `tests/Typographer.Tests/Corpus/CorpusFileTests.cs`:

```csharp
using Typographer.Rules;

namespace Typographer.Tests.Corpus;

/// <summary>Golden-корпус: выход типографа на файле совпадает с эталоном рядом с ним.</summary>
/// <remarks>
/// Эталоны перегенерируются переменной окружения TYPOGRAPHER_UPDATE_CORPUS=1. Тест при
/// этом ПАДАЕТ намеренно: перегенерация — не проверка, и зелёный прогон с перезаписанными
/// эталонами обманул бы того, кто её запустил и забыл про переменную.
/// Набор правил — Default в режиме HTML: корпус показывает то, что получает тот, кто просто
/// вызвал Typograf.Html(text). Всё, что вне Default, проверяется юнит-тестами правил.
/// </remarks>
public class CorpusFileTests
{
    private static readonly bool Update =
        Environment.GetEnvironmentVariable("TYPOGRAPHER_UPDATE_CORPUS") == "1";

    [Theory]
    [MemberData(nameof(CorpusFiles.Names), MemberType = typeof(CorpusFiles))]
    public void OutputMatchesGolden(string name)
    {
        var typograf = new HtmlTypograf(new HtmlOptions { Rules = RuleSet.Default });
        string actual = typograf.Process(CorpusFiles.ReadInput(name));

        if (Update)
        {
            CorpusFiles.WriteExpected(name, actual);
            Assert.Fail($"Эталон {name}.out.txt перезаписан. Сними TYPOGRAPHER_UPDATE_CORPUS и проверь diff.");
        }

        Assert.Equal(CorpusFiles.ReadExpected(name), actual);
    }

    // Гарантия 5 на связном материале, а не на строчках-ловушках: корпус — единственное
    // место, где вход похож на настоящий документ.
    [Theory]
    [MemberData(nameof(CorpusFiles.Names), MemberType = typeof(CorpusFiles))]
    public void GoldenOutputIsFixedPoint(string name)
    {
        var typograf = new HtmlTypograf(new HtmlOptions { Rules = RuleSet.Default });
        string expected = CorpusFiles.ReadExpected(name);

        Assert.Equal(expected, typograf.Process(expected));
    }

    [Fact]
    public void CorpusIsNotEmpty() => Assert.NotEmpty(CorpusFiles.EnumerateNames());
}
```

- [ ] **Step 5: Запретить git менять переводы строк в корпусе**

Создать `.gitattributes` в корне:

```
* text=auto eol=lf
tests/Typographer.Corpus/*.txt text eol=lf
```

- [ ] **Step 6: Сгенерировать эталоны и проверить их глазами**

```bash
rtk env TYPOGRAPHER_UPDATE_CORPUS=1 dotnet test tests/Typographer.Tests --filter-class Typographer.Tests.Corpus.CorpusFileTests
```

Под Windows PowerShell то же самое: `$env:TYPOGRAPHER_UPDATE_CORPUS=1; dotnet test ...;
$env:TYPOGRAPHER_UPDATE_CORPUS=$null`.

Тест обязан упасть с сообщением о перезаписи. Затем прочитать каждый `*.out.txt` и убедиться,
что вывод правильный. Неразрывный пробел в файле невидим — смотреть через
`rtk git diff --word-diff-regex=. ` или ищя ` ` грепом. Всё, что выглядит неверным, —
дефект правила, а не корпуса: чинить правило, а не эталон.

- [ ] **Step 7: Прогнать тест без переменной**

```bash
dotnet test tests/Typographer.Tests
```

Ожидается: всё зелено, число тестов выросло на 21 (10 случаев × 2 теории + 1 факт).

- [ ] **Step 8: Коммит**

```bash
rtk git add tests/Typographer.Corpus tests/Typographer.Tests .gitattributes
rtk git commit -m "test: golden-корпус в файлах"
```

---

## Task 2: Каждое правило реестра в одиночку

**Files:**
- Create: `tests/Typographer.Tests/Rules/EveryRuleTests.cs`

**Interfaces:**
- Consumes: `CorpusFiles` из задачи 1, `HardCases.All`, `RuleId.Registry.All`,
  `RuleId.Registry.MarkupChanging`, `RuleSet.None`, `RuleSet.FromPhase`.
- Produces: ничего для кода.

Смысл задачи. Юнит-тест правила проверяет правило в наборе, где рядом стоят его соседи.
Реестровый прогон проверяет обратное: правило, включённое ОДНО, ведёт себя как обещано —
не падает, идемпотентно, не трогает разметку и не меняет текст, в котором его знаков нет.
107 правил × корпус — четыре свойства, которые никто не проверяет поштучно.

- [ ] **Step 1: Написать тест**

Создать `tests/Typographer.Tests/Rules/EveryRuleTests.cs`:

```csharp
using Typographer.Internal;
using Typographer.Rules;
using Typographer.Tests.Corpus;

namespace Typographer.Tests.Rules;

/// <summary>Свойства, которые обязано соблюдать КАЖДОЕ правило реестра, включённое в одиночку.</summary>
/// <remarks>
/// Юнит-тесты проверяют правило среди соседей; здесь у правила соседей нет. Разница не
/// теоретическая: правило, полагающееся на пробел, дописанный соседом, в одиночку ведёт себя
/// иначе, и узнать об этом лучше от теста, чем от пользователя с нестандартным набором.
/// </remarks>
public class EveryRuleTests
{
    public static TheoryData<string> RuleNames
    {
        get
        {
            var data = new TheoryData<string>();
            foreach (RuleId rule in RuleId.Registry.All)
            {
                data.Add(rule.Name);
            }

            return data;
        }
    }

    /// <summary>Входы: файловый корпус плюс строчки-ловушки из HardCases.</summary>
    private static IEnumerable<string> Inputs()
    {
        foreach (string name in CorpusFiles.EnumerateNames())
        {
            yield return CorpusFiles.ReadInput(name);
        }

        foreach (object?[] row in HardCases.All)
        {
            yield return (string)row[0]!;
        }
    }

    private static RuleSet Single(string name)
    {
        Assert.True(RuleId.TryParse(name, out RuleId rule));
        return RuleSet.None.With(rule);
    }

    [Theory]
    [MemberData(nameof(RuleNames))]
    public void RuleAloneDoesNotThrow(string name)
    {
        var html = new HtmlTypograf(new HtmlOptions { Rules = Single(name) });
        var text = new TextTypograf(new TextOptions { Rules = Single(name) });

        foreach (string source in Inputs())
        {
            Assert.NotNull(html.Process(source));
            Assert.NotNull(text.Process(source));
        }
    }

    // Идемпотентность каждого правила в одиночку. Исключения — те же три, что в гарантии 5:
    // replaceNbsp снимает авторский неразрывный пробел, nbr вкладывает теги переноса,
    // escape по определению неподвижной точки не имеет.
    [Theory]
    [MemberData(nameof(RuleNames))]
    public void RuleAloneIsIdempotent(string name)
    {
        if (name is "common/nbsp/replaceNbsp" or "common/html/nbr" or "common/html/escape")
        {
            return;
        }

        var typograf = new HtmlTypograf(new HtmlOptions { Rules = Single(name) });

        foreach (string source in Inputs())
        {
            string once = typograf.Process(source);
            Assert.Equal(once, typograf.Process(once));
        }
    }

    // Гарантия 3: разметка выходит байт-в-байт. Правила, которые разметку создают или
    // преобразуют намеренно, перечислены реестром и из проверки исключены.
    [Theory]
    [MemberData(nameof(RuleNames))]
    public void RuleAloneKeepsMarkupIntact(string name)
    {
        Assert.True(RuleId.TryParse(name, out RuleId rule));
        if (Array.IndexOf(RuleId.Registry.MarkupChanging, rule) >= 0)
        {
            return;
        }

        var typograf = new HtmlTypograf(new HtmlOptions { Rules = RuleSet.None.With(rule) });

        foreach (string source in Inputs())
        {
            Assert.Equal(Tags(source), Tags(typograf.Process(source)));
        }
    }

    // Правило, которому в тексте не за что зацепиться, обязано вернуть исходный экземпляр
    // (гарантия 6). Текст подобран так, что в нём нет ни одного знака, интересного хотя бы
    // одному правилу: только буквы и одиночные пробелы, все слова длиннее трёх букв.
    [Theory]
    [MemberData(nameof(RuleNames))]
    public void RuleAloneLeavesNeutralTextAlone(string name)
    {
        const string Neutral = "простое предложение написанное словами длиннее трёх букв";
        var typograf = new HtmlTypograf(new HtmlOptions { Rules = Single(name) });

        Assert.Same(Neutral, typograf.Process(Neutral));
    }

    // Каждое правило заявляет фазу, и RuleSet.FromPhase обязан его в этой фазе найти:
    // фаза — это то, по чему конвейер решает, включать ли проход вообще.
    [Theory]
    [MemberData(nameof(RuleNames))]
    public void RuleBelongsToItsPhase(string name)
    {
        Assert.True(RuleId.TryParse(name, out RuleId rule));

        Assert.True(RuleSet.FromPhase(rule.Phase).Contains(rule));
    }

    /// <summary>Все сегменты разметки подряд — то, что гарантия 3 обещает не менять.</summary>
    private static string Tags(string html)
    {
        var tags = new System.Text.StringBuilder();
        int index = 0;
        while (index < html.Length)
        {
            if (html[index] == '<' && MarkupScanner.TryRead(html.AsSpan(), index, out int length))
            {
                tags.Append(html, index, length).Append('\n');
                index += length;
                continue;
            }

            index++;
        }

        return tags.ToString();
    }
}
```

- [ ] **Step 2: Согласовать со сканером разметки**

Метод `Tags` вызывает `MarkupScanner`. Точную сигнатуру взять из
`tests/Typographer.Tests/Guarantees/MarkupIntegrityTests.cs`, где такая же выборка тегов уже
написана, и повторить её один в один — если там вспомогательный метод уже есть, вынести его в
общий `internal static` класс и использовать в обоих местах, а не копировать.

- [ ] **Step 3: Запустить и починить найденное**

```bash
dotnet test tests/Typographer.Tests --filter-class Typographer.Tests.Rules.EveryRuleTests
```

Каждое падение разбирается по существу. Три исхода допустимы:

| Что нашлось | Что делать |
|---|---|
| Правило падает или ломает разметку в одиночку | Дефект правила. Чинить `src/`. |
| Правило неидемпотентно в одиночку, но идемпотентно в `Default` | Дефект правила: `Default` не обязан его спасать. |
| Свойство неверно сформулировано | Уточнить тест и записать причину комментарием. |

Список исключений расширять только с письменной причиной в комментарии — молчаливое
исключение обесценивает весь тест.

- [ ] **Step 4: Коммит**

```bash
rtk git add tests/Typographer.Tests/Rules/EveryRuleTests.cs
rtk git commit -m "test: свойства каждого правила реестра в одиночку"
```

---

## Task 3: Расхождения с оракулом закреплены таблицей

**Files:**
- Create: `tests/Typographer.Tests/Oracle/OracleSnapshot.cs`
- Create: `tests/Typographer.Tests/Oracle/OracleSnapshotTests.cs`
- Create: `docs/oracle/divergences.md`
- Modify: `tests/Typographer.Tests/Typographer.Tests.csproj`

**Interfaces:**
- Consumes: `docs/oracle/lebedev.md` — снимок, снятый `tools/oracle-snapshot.sh`.
- Produces: `OracleSnapshot.Rows` — `IReadOnlyList<(string Input, string Expected)>`,
  используется задачей 4.

Смысл задачи. Сегодня расхождения с Лебедевым описаны прозой в `docs/oracle/lebedev.md`:
«мы ставим узкий пробел после №», «мы не оборачиваем телефон в nobr». Проза не падает, когда
правило меняют. Таблица падает. Сеть при этом не нужна: снимок закоммичен.

- [ ] **Step 1: Передать путь к снимку в сборку тестов**

Добавить в `tests/Typographer.Tests/Typographer.Tests.csproj` в тот же `ItemGroup`, что и
`CorpusDirectory`:

```xml
    <AssemblyAttribute Include="System.Reflection.AssemblyMetadataAttribute">
      <_Parameter1>OracleSnapshot</_Parameter1>
      <_Parameter2>$(MSBuildThisFileDirectory)../../docs/oracle/lebedev.md</_Parameter2>
    </AssemblyAttribute>
```

- [ ] **Step 2: Написать разбор снимка**

Создать `tests/Typographer.Tests/Oracle/OracleSnapshot.cs`:

```csharp
using System.Reflection;

namespace Typographer.Tests.Oracle;

/// <summary>Снимок веб-сервиса Лебедева, разобранный из таблицы docs/oracle/lebedev.md.</summary>
/// <remarks>
/// Снимок снят скриптом tools/oracle-snapshot.sh с настройками entityType=3 (символы),
/// useBr=false, useP=false, maxNobr=0. В таблице неразрывный пробел записан подчёркиванием,
/// вертикальная черта экранирована, а угловые скобки и амперсанд — сущностями: иначе
/// Markdown-таблица разъезжается. Разбор возвращает всё это к настоящим символам.
/// </remarks>
public static class OracleSnapshot
{
    /// <summary>Пары «вход — выход оракула» в порядке таблицы.</summary>
    public static IReadOnlyList<(string Input, string Expected)> Rows { get; } = Parse();

    private static IReadOnlyList<(string, string)> Parse()
    {
        string path = Path.GetFullPath(
            typeof(OracleSnapshot).Assembly
                .GetCustomAttributes<AssemblyMetadataAttribute>()
                .Single(a => a.Key == "OracleSnapshot")
                .Value!);

        var rows = new List<(string, string)>();
        foreach (string line in File.ReadLines(path))
        {
            if (!line.StartsWith("| ", StringComparison.Ordinal) || line.StartsWith("|---", StringComparison.Ordinal))
            {
                continue;
            }

            // Разделитель — вертикальная черта, не экранированная обратным слешем.
            string body = line.Trim();
            body = body[1..^1];
            int split = FindSeparator(body);
            if (split < 0)
            {
                continue;
            }

            string input = Unescape(body[..split]);
            string expected = Unescape(body[(split + 1)..]);
            if (input == "Вход")
            {
                continue;
            }

            rows.Add((input, expected));
        }

        return rows;
    }

    private static int FindSeparator(string body)
    {
        for (int i = 0; i < body.Length; i++)
        {
            if (body[i] == '|' && (i == 0 || body[i - 1] != '\\'))
            {
                return i;
            }
        }

        return -1;
    }

    private static string Unescape(string cell) => cell
        .Trim()
        .Replace("\\|", "|")
        .Replace("_", " ")
        .Replace("&lt;", "<")
        .Replace("&gt;", ">")
        .Replace("&amp;", "&");
}
```

Замена подчёркивания на неразрывный пробел безусловна: в снимке нет ни одного входа с
настоящим подчёркиванием, и проверить это — часть шага 4.

- [ ] **Step 3: Написать тест сверки**

Создать `tests/Typographer.Tests/Oracle/OracleSnapshotTests.cs`:

```csharp
using Typographer.Rules;

namespace Typographer.Tests.Oracle;

/// <summary>Наш вывод против вывода оракула Лебедева на закоммиченном снимке.</summary>
/// <remarks>
/// Сеть не трогается: снимок лежит в docs/oracle/lebedev.md. Живая сверка — отдельный
/// проект tests/Typographer.Oracle, вне CI.
/// Строки, где мы расходимся с оракулом НАМЕРЕННО, перечислены в Divergences с причиной.
/// Тест падает в обе стороны: и когда совпадение сломалось, и когда расхождение из списка
/// внезапно исчезло — второе значит, что список устарел и вводит в заблуждение.
/// Набор правил — Lebedev: сверяться с оракулом имеет смысл в пресете, который его повторяет.
/// </remarks>
public class OracleSnapshotTests
{
    /// <summary>Входы, на которых мы расходимся с оракулом намеренно. Ключ — вход, значение — причина.</summary>
    private static readonly Dictionary<string, string> Divergences = new(StringComparer.Ordinal)
    {
        // Заполняется шагом 4 по фактическому результату прогона.
    };

    public static TheoryData<string> Inputs
    {
        get
        {
            var data = new TheoryData<string>();
            foreach ((string input, _) in OracleSnapshot.Rows)
            {
                data.Add(input);
            }

            return data;
        }
    }

    [Theory]
    [MemberData(nameof(Inputs))]
    public void MatchesOracleUnlessDivergenceIsRecorded(string input)
    {
        string expected = OracleSnapshot.Rows.First(row => row.Input == input).Expected;
        string actual = new HtmlTypograf(new HtmlOptions
        {
            Rules = RuleSet.Lebedev,
            Entities = EntityMode.Symbols,
        }).Process(input);

        if (Divergences.TryGetValue(input, out string? reason))
        {
            Assert.False(
                expected == actual,
                $"Расхождение исчезло, а запись о нём осталась: «{input}». Причина в списке: {reason}");
            return;
        }

        Assert.Equal(expected, actual);
    }

    [Fact]
    public void SnapshotIsNotEmpty() => Assert.NotEmpty(OracleSnapshot.Rows);

    // Разбор снимка подменяет подчёркивание неразрывным пробелом. Приём держится на том, что
    // настоящего подчёркивания во входах нет — если появится, разбор начнёт врать молча.
    [Fact]
    public void NoInputContainsUnderscore()
        => Assert.DoesNotContain(OracleSnapshot.Rows, row => row.Input.Contains('_'));
}
```

- [ ] **Step 4: Заполнить список расхождений**

```bash
dotnet test tests/Typographer.Tests --filter-class Typographer.Tests.Oracle.OracleSnapshotTests
```

Каждое падение разбирается ПООТДЕЛЬНОСТИ, и решение принимается по источникам в порядке
приоритета из спецификации: ГОСТ Р 7.0.110-2025, затем Мильчин, затем практика Лебедева,
затем JS-typograf.

| Что показал разбор | Что делать |
|---|---|
| Оракул прав, мы ошибаемся | Чинить правило. Расхождение в список НЕ вносить. |
| Мы правы, оракул делает по-своему | Внести в `Divergences` с однострочной причиной. |
| Разница в разметке оракула (`<nobr class="phone">`) | Внести с причиной: чужую разметку мы не воспроизводим. |
| Разница в сущностях или экранировании | Проверить нормализацию разбора; чинить разбор, а не список. |

Причина в списке — одна строка по существу, вида
`«мы ставим узкий пробел после №: ГОСТ 7.0.110, п. 5.3»`, а не «отличается».

- [ ] **Step 5: Перенести прозу в документ расхождений**

Создать `docs/oracle/divergences.md` — человекочитаемое сопровождение к списку в коде:
заголовок, абзац о том, что список машинно проверяется тестом `OracleSnapshotTests`, и
таблица «вход — наш вывод — вывод оракула — причина», заполненная по результату шага 4.
Из `docs/oracle/lebedev.md` разделы «Что закрыл план 2b/2c/2d» не удалять: они описывают
историю, а не текущие расхождения.

- [ ] **Step 6: Прогнать весь набор и закоммитить**

```bash
dotnet test tests/Typographer.Tests
rtk git add tests/Typographer.Tests docs/oracle
rtk git commit -m "test: расхождения с оракулом Лебедева закреплены таблицей"
```

---

## Task 4: Живая сверка с веб-сервисом

**Files:**
- Create: `tests/Typographer.Oracle/Typographer.Oracle.csproj`
- Create: `tests/Typographer.Oracle/LebedevService.cs`
- Create: `tests/Typographer.Oracle/LiveOracleTests.cs`
- Modify: `Typographer.slnx`

**Interfaces:**
- Consumes: `tools/oracle-inputs.txt` — те же входы, что снимает скрипт снимка.
- Produces: ничего для кода.

Смысл задачи: снимок устаревает молча. Живой тест отвечает на один вопрос — изменился ли
сервис с тех пор, как снимок сняли. Он ходит в сеть, поэтому в CI не запускается: без
переменной окружения `TYPOGRAPHER_ORACLE=1` тесты помечают себя пропущенными. Фильтровать по
трейту в командной строке ненадёжнее — забытый фильтр означает сеть в CI, а забытая
переменная означает всего лишь пропуск.

- [ ] **Step 1: Создать проект**

Создать `tests/Typographer.Oracle/Typographer.Oracle.csproj`:

```xml
<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <OutputType>Exe</OutputType>
    <IsPackable>false</IsPackable>
    <TestingPlatformDotnetTestSupport>true</TestingPlatformDotnetTestSupport>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="xunit.v3" Version="3.2.0" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="../../src/Typographer/Typographer.csproj" />
  </ItemGroup>

  <ItemGroup>
    <Using Include="Xunit" />
    <AssemblyAttribute Include="System.Reflection.AssemblyMetadataAttribute">
      <_Parameter1>RepositoryRoot</_Parameter1>
      <_Parameter2>$(MSBuildThisFileDirectory)../..</_Parameter2>
    </AssemblyAttribute>
  </ItemGroup>

</Project>
```

Добавить проект в `Typographer.slnx` в папку `/tests/`:

```xml
    <Project Path="tests/Typographer.Oracle/Typographer.Oracle.csproj" />
```

- [ ] **Step 2: Написать клиент сервиса**

Создать `tests/Typographer.Oracle/LebedevService.cs`:

```csharp
using System.Net.Http;
using System.Net.Http.Headers;
using System.Text;
using System.Xml.Linq;

namespace Typographer.Oracle;

/// <summary>Клиент SOAP-сервиса typograf.artlebedev.ru.</summary>
/// <remarks>
/// Параметры повторяют tools/oracle-snapshot.sh: entityType=3 — символы, а не сущности;
/// useBr, useP выключены; maxNobr=0. Иначе живая сверка сравнивала бы снимок с выводом,
/// снятым при других настройках.
/// Сервис чужой: между запросами выдерживается секунда.
/// </remarks>
public sealed class LebedevService : IDisposable
{
    private const string Endpoint = "https://typograf.artlebedev.ru/webservices/typograf.asmx";
    private const string Namespace = "http://typograf.artlebedev.ru/webservices/";

    private readonly HttpClient _http = new() { Timeout = TimeSpan.FromSeconds(30) };

    public async Task<string> ProcessTextAsync(string text)
    {
        string body =
            "<?xml version=\"1.0\" encoding=\"utf-8\"?>" +
            "<soap:Envelope xmlns:soap=\"http://schemas.xmlsoap.org/soap/envelope/\"><soap:Body>" +
            $"<ProcessText xmlns=\"{Namespace}\">" +
            $"<text>{Escape(text)}</text>" +
            "<entityType>3</entityType><useBr>false</useBr><useP>false</useP><maxNobr>0</maxNobr>" +
            "</ProcessText></soap:Body></soap:Envelope>";

        using var content = new StringContent(body, Encoding.UTF8);
        content.Headers.ContentType = new MediaTypeHeaderValue("text/xml") { CharSet = "utf-8" };

        using var request = new HttpRequestMessage(HttpMethod.Post, Endpoint) { Content = content };
        request.Headers.Add("SOAPAction", $"\"{Namespace}ProcessText\"");

        using HttpResponseMessage response = await _http.SendAsync(request);
        response.EnsureSuccessStatusCode();

        string xml = await response.Content.ReadAsStringAsync();
        XElement result = XDocument.Parse(xml).Descendants()
            .First(e => e.Name.LocalName == "ProcessTextResult");

        await Task.Delay(TimeSpan.FromSeconds(1));
        return result.Value.Trim();
    }

    public void Dispose() => _http.Dispose();

    private static string Escape(string text)
        => text.Replace("&", "&amp;").Replace("<", "&lt;").Replace(">", "&gt;");
}
```

- [ ] **Step 3: Написать тест живой сверки**

Создать `tests/Typographer.Oracle/LiveOracleTests.cs`:

```csharp
using System.Reflection;

namespace Typographer.Oracle;

/// <summary>Живая сверка снимка с веб-сервисом. Вне CI: ходит в сеть.</summary>
/// <remarks>
/// Запуск: TYPOGRAPHER_ORACLE=1 dotnet test tests/Typographer.Oracle.
/// Без переменной тест помечает себя пропущенным — так забытый фильтр в командной строке
/// не приводит к сети в CI.
/// Тест отвечает на один вопрос: изменился ли сервис с тех пор, как снимок сняли. Наши
/// расхождения с оракулом проверяет OracleSnapshotTests в основном наборе, без сети.
/// </remarks>
[Trait("Category", "Oracle")]
public class LiveOracleTests
{
    private static string Root => Path.GetFullPath(
        typeof(LiveOracleTests).Assembly
            .GetCustomAttributes<AssemblyMetadataAttribute>()
            .Single(a => a.Key == "RepositoryRoot")
            .Value!);

    [Fact]
    public async Task SnapshotStillMatchesLiveService()
    {
        Assert.SkipUnless(
            Environment.GetEnvironmentVariable("TYPOGRAPHER_ORACLE") == "1",
            "Живая сверка ходит в сеть. Задай TYPOGRAPHER_ORACLE=1, чтобы её запустить.");

        string[] inputs = await File.ReadAllLinesAsync(Path.Combine(Root, "tools", "oracle-inputs.txt"));
        Dictionary<string, string> snapshot = ReadSnapshot();

        using var service = new LebedevService();
        var stale = new List<string>();

        foreach (string input in inputs)
        {
            if (string.IsNullOrWhiteSpace(input) || !snapshot.TryGetValue(input, out string? recorded))
            {
                continue;
            }

            string live = await service.ProcessTextAsync(input);
            if (live != recorded)
            {
                stale.Add($"вход: {input}\n  снимок: {recorded}\n  сервис: {live}");
            }
        }

        Assert.True(
            stale.Count == 0,
            $"Снимок устарел на {stale.Count} строках. Пересними tools/oracle-snapshot.sh.\n"
                + string.Join("\n", stale));
    }

    /// <summary>Читает снимок так же, как OracleSnapshot в основном наборе, но без ссылки на него.</summary>
    private static Dictionary<string, string> ReadSnapshot()
    {
        var rows = new Dictionary<string, string>(StringComparer.Ordinal);
        foreach (string line in File.ReadLines(Path.Combine(Root, "docs", "oracle", "lebedev.md")))
        {
            if (!line.StartsWith("| ", StringComparison.Ordinal) || line.StartsWith("|---", StringComparison.Ordinal))
            {
                continue;
            }

            string[] cells = line.Trim()[1..^1].Split(" | ");
            if (cells.Length != 2 || cells[0].Trim() == "Вход")
            {
                continue;
            }

            rows[cells[0].Trim().Replace("\\|", "|")] =
                cells[1].Trim().Replace("\\|", "|").Replace("_", " ");
        }

        return rows;
    }
}
```

Дублирование разбора снимка между проектами намеренное: тянуть ссылку из `Typographer.Oracle`
на `Typographer.Tests` только ради двадцати строк разбора — связь дороже дубля.

- [ ] **Step 4: Проверить оба режима**

```bash
dotnet test tests/Typographer.Oracle
```

Ожидается: 1 тест, пропущен, с сообщением про переменную окружения.

```bash
rtk env TYPOGRAPHER_ORACLE=1 dotnet test tests/Typographer.Oracle
```

Ожидается: тест выполнился и либо прошёл, либо перечислил устаревшие строки. Если сервис
недоступен — записать это в отчёт задачи и не чинить: недоступность чужого сервиса не
дефект нашего кода.

- [ ] **Step 5: Убедиться, что CI не ходит в сеть**

```bash
dotnet test
```

Ожидается: набор `Typographer.Oracle` показал 1 пропущенный тест, сети не было.

- [ ] **Step 6: Коммит**

```bash
rtk git add tests/Typographer.Oracle Typographer.slnx
rtk git commit -m "test: живая сверка с веб-сервисом Лебедева вне CI"
```

---

## Task 5: Фаззинг

**Files:**
- Create: `fuzz/Typographer.Fuzz/Typographer.Fuzz.csproj`
- Create: `fuzz/Typographer.Fuzz/Program.cs`
- Create: `.github/workflows/fuzz.yml`
- Modify: `Typographer.slnx`
- Modify: `.gitignore`

**Interfaces:**
- Consumes: публичный API `HtmlTypograf`, `TextTypograf`; затравка — `tests/Typographer.Corpus/*.in.txt`.
- Produces: артефакт `fuzz-crashes` в workflow.

- [ ] **Step 1: Создать цель фаззинга**

Создать `fuzz/Typographer.Fuzz/Typographer.Fuzz.csproj`:

```xml
<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="SharpFuzz" Version="2.2.0" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="../../src/Typographer/Typographer.csproj" />
  </ItemGroup>

</Project>
```

Создать `fuzz/Typographer.Fuzz/Program.cs`:

```csharp
using SharpFuzz;
using Typographer;
using Typographer.Rules;

// Цель фаззинга: конвейер не имеет права выбросить исключение ни на каком входе, кроме
// OutputTooLargeException — единственного объявленного (гарантия 2, docs/spec.md).
// Проверяются оба режима и оба крайних набора правил: None ловит дефекты каркаса,
// All — дефекты правил. Идемпотентность здесь не проверяется: её нарушают три правила
// по своей природе, и фаззеру про это знать негде.
var html = new HtmlTypograf(new HtmlOptions { Rules = RuleSet.All });
var htmlBare = new HtmlTypograf(new HtmlOptions { Rules = RuleSet.None });
var text = new TextTypograf(new TextOptions { Rules = RuleSet.All });

Fuzzer.OutOfProcess.Run(stream =>
{
    using var reader = new StreamReader(stream);
    string source = reader.ReadToEnd();

    html.Process(source);
    htmlBare.Process(source);
    text.Process(source);
});
```

Если `Fuzzer.OutOfProcess.Run` в установленной версии SharpFuzz отсутствует или требует
libFuzzer на машине сборки, использовать `Fuzzer.LibFuzzer.Run(ReadOnlySpan<byte> bytes => ...)`
и декодировать вход `Encoding.UTF8.GetString(bytes)`. Точную форму взять из README пакета
той версии, которая встала — гадать по памяти нельзя.

Добавить проект в `Typographer.slnx` в новую папку `/fuzz/`.

- [ ] **Step 2: Собрать локально**

```bash
rtk dotnet build fuzz/Typographer.Fuzz -c Release
```

Ожидается: сборка проходит. Запустить фаззер под Windows не получится — libFuzzer есть
только на Linux, и запуск делает workflow.

- [ ] **Step 3: Написать workflow**

Создать `.github/workflows/fuzz.yml`:

```yaml
name: Fuzz

on:
  workflow_dispatch:
  schedule:
    # Раз в неделю, в ночь на понедельник. Фаззинг долгий и в PR не нужен.
    - cron: '0 3 * * 1'

jobs:
  fuzz:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Установить sharpfuzz
        run: dotnet tool install --global SharpFuzz.CommandLine

      - name: Собрать цель
        run: dotnet publish fuzz/Typographer.Fuzz -c Release -o fuzz/out

      - name: Инструментировать ядро
        run: sharpfuzz fuzz/out/Typographer.dll

      - name: Подготовить затравку
        run: |
          mkdir -p fuzz/seed
          cp tests/Typographer.Corpus/*.in.txt fuzz/seed/

      - name: Запустить libFuzzer
        run: |
          dotnet tool install --global libFuzzerDotnet || true
          cd fuzz
          timeout 600 out/Typographer.Fuzz seed -max_total_time=540 -timeout=10 || true

      - name: Сохранить падения
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: fuzz-crashes
          path: fuzz/crash-*
          if-no-files-found: ignore
```

Шаги запуска libFuzzer сверить с README SharpFuzz той версии, которая встала: у пакета
менялся способ запуска (`libfuzzer-dotnet` против `Fuzzer.OutOfProcess`), и переписывать
workflow по памяти нельзя. Порядок действий при расхождении — открыть README пакета,
привести шаги к нему, запустить workflow и убедиться, что он зелёный.

Добавить в `.gitignore`:

```
fuzz/out/
fuzz/seed/
fuzz/crash-*
```

- [ ] **Step 4: Запустить workflow и довести до зелёного**

```bash
rtk git add fuzz .github/workflows/fuzz.yml Typographer.slnx .gitignore
rtk git commit -m "test: фаззинг SharpFuzz по расписанию"
rtk git push
rtk gh workflow run Fuzz --ref <текущая ветка>
rtk gh run watch
```

Workflow обязан завершиться зелёным. Красный workflow, оставленный «на потом», ничем не
лучше отсутствующего: он не запускается по расписанию, а просто шлёт письма.

Если фаззер нашёл падение — это баг ядра. Вход из артефакта добавляется в `HardCases`
отдельным тестом, дефект чинится в `src/`, и только потом задача считается сделанной.

- [ ] **Step 5: Коммит правок workflow**

```bash
rtk git add .github/workflows/fuzz.yml fuzz
rtk git commit -m "ci: фаззинг доведён до зелёного прогона"
```

---

## Task 6: CI обязан выполняться

**Files:**
- Modify: `.github/workflows/ci.yml`
- Modify: `README.md`

Смысл задачи. Проверка `build` объявлена обязательной в правилах ветки, но не выполнялась
ни разу: в репозитории один прогон, ручной, от 8 сентября на `master`. Пять пушей в
`develop` не породили ни одного запуска. Пока причина не найдена, обязательная проверка —
фикция, и каждый пуш в `develop` обходит правило с предупреждением.

- [ ] **Step 1: Установить причину**

```bash
rtk gh run list --limit 20 --json databaseId,headBranch,event,conclusion,createdAt
rtk gh api repos/gberikov/Typographer/actions/workflows
rtk gh api repos/gberikov/Typographer/actions/permissions
rtk gh api "repos/gberikov/Typographer/commits/develop/check-runs"
```

Проверить по порядку: workflow активен (`state: active`), Actions разрешены, файл
`.github/workflows/ci.yml` есть в ветке `develop` и содержит `develop` в списке `branches`,
у ветки нет правила, отменяющего запуск. Записать найденное в отчёт задачи.

- [ ] **Step 2: Проверить эмпирически**

```bash
rtk gh workflow run CI --ref develop
rtk gh run watch
```

Если ручной запуск проходит, а пуш — нет, дело в триггере, а не в сборке. Тогда сравнить
`on:` в файле с документацией GitHub и привести к рабочему виду; самая частая причина —
`branches`, не совпадающие с именем ветки, и `paths`-фильтры.

- [ ] **Step 3: Добить до зелёного пуша**

Сделать пуш в текущую ветку плана и убедиться, что прогон появился и завершился успешно:

```bash
rtk git push
rtk gh run list --limit 3
```

Ветка плана в `on.push.branches` не перечислена — добавить `pull_request` уже есть, поэтому
проверить проще через PR или временно расширить триггер до `branches: ['**']` и вернуть
обратно после проверки. Решение записать в отчёт.

- [ ] **Step 4: Прогонять тесты во всех целевых платформах**

CI сейчас запускает `dotnet test -c Release`, а тестовый проект собирается только под
`net10.0`. Ядро при этом многоцелевое, и `netstandard2.0` в CI не проверяется ничем, кроме
компиляции. Добавить в workflow шаг сборки всех целей:

```yaml
      - run: dotnet build -c Release
      - run: dotnet test -c Release
```

- [ ] **Step 5: Значок сборки в README**

Добавить первой строкой после заголовка `README.md`:

```markdown
[![CI](https://github.com/gberikov/Typographer/actions/workflows/ci.yml/badge.svg)](https://github.com/gberikov/Typographer/actions/workflows/ci.yml)
```

- [ ] **Step 6: Коммит**

```bash
rtk git add .github/workflows/ci.yml README.md
rtk git commit -m "ci: сборка проверяется на каждом пуше"
```

---

## Task 7: Замер производительности после смены фазы Bind

**Files:**
- Create: `docs/perf.md`
- Modify: `bench/Typographer.Bench/TypografBenchmarks.cs` (при необходимости)

Смысл задачи. В плане 2c фаза Bind перестала точечно править буфер и стала писать документ
целиком. Спецификация обещает 50 млн символов в секунду; после смены подхода замера не было.
Цифра, которую никто не мерил, — не цель, а пожелание.

- [ ] **Step 1: Прогнать бенчмарк**

```bash
rtk dotnet run -c Release --project bench/Typographer.Bench
```

- [ ] **Step 2: Записать результат**

Создать `docs/perf.md`: машина, версия SDK, дата, таблица результатов BenchmarkDotNet,
вычисленная пропускная способность в символах в секунду и явный ответ — достигнута цель
спецификации или нет. Если не достигнута, записать разрыв числом и назвать место, где он
возник; чинить производительность в этом плане не нужно, нужно перестать про неё гадать.

- [ ] **Step 3: Свериться с обещанием об аллокациях**

`MemoryDiagnoser` уже включён. Проверить строку `Allocated` для пути
`Process(ReadOnlySpan<char>, IBufferWriter<char>)`: спецификация обещает ноль байт.
Расхождение записать в `docs/perf.md` тем же способом.

- [ ] **Step 4: Коммит**

```bash
rtk git add docs/perf.md bench
rtk git commit -m "docs: замер производительности после перевода Bind в пишущий проход"
```

---

## Task 8: Документация

**Files:**
- Modify: `docs/spec.md`
- Modify: `docs/superpowers/plans/2026-09-08-typographer-core.md`
- Modify: `README.md`

- [ ] **Step 1: Привести раздел 9 спецификации в соответствие с тем, что есть**

Раздел 9 описывает четыре уровня тестирования. Дописать к нему, как их запускать:

```markdown
Как запускать:

| Уровень | Команда |
|---|---|
| Всё, что идёт в CI | `dotnet test` |
| Перегенерация golden-корпуса | `TYPOGRAPHER_UPDATE_CORPUS=1 dotnet test tests/Typographer.Tests` |
| Живая сверка с оракулом | `TYPOGRAPHER_ORACLE=1 dotnet test tests/Typographer.Oracle` |
| Фаззинг | workflow `Fuzz`, по расписанию и вручную |

Расхождения с оракулом Лебедева перечислены в `docs/oracle/divergences.md` и закреплены
тестом `OracleSnapshotTests`: список падает и когда совпадение сломалось, и когда
расхождение из списка исчезло.
```

- [ ] **Step 2: Отметить план 3 выполненным**

В `docs/superpowers/plans/2026-09-08-typographer-core.md`, в таблице «Очередь планов»,
пометить строку плана 3 как выполненную тем же способом, каким помечены планы 1, 2a–2d.

- [ ] **Step 3: Раздел о запуске тестов в README**

Короткий раздел «Тесты» с той же таблицей команд. Читатель README не обязан открывать
спецификацию, чтобы узнать, как прогнать тесты.

- [ ] **Step 4: Коммит**

```bash
rtk git add docs README.md
rtk git commit -m "docs: как запускать четыре уровня тестирования"
```

---

## Самопроверка плана

**Покрытие очереди планов.** Строка «3. Качество» обещает четыре вещи:

| Обещано | Задача |
|---|---|
| Golden-корпус в файлах | 1 |
| Оракул `typograf.artlebedev.ru` | 3 (без сети) и 4 (живой) |
| Фаззинг SharpFuzz | 5 |
| Тест на перестановку правил внутри фазы | 2 |

Задачи 6, 7 и 8 обещаниями очереди не покрыты и добавлены сознательно: они закрывают долги,
накопленные планами 1–2d, и по смыслу относятся к качеству — неработающий CI и неизмеренная
производительность обесценивают всё остальное в этом плане.

**Про «перестановку правил внутри фазы».** Порядок правил в наборе не задаётся: `RuleSet` —
битовая маска, порядок применения зашит в диспетчеры фаз. Переставлять нечего, и задача 2
проверяет то, ради чего перестановку и хотели проверять: правило не должно зависеть от
соседей по фазе. Прогон каждого правила в одиночку — более сильное утверждение, чем
перестановка пар.

**Чего в плане нет и почему.**

| Не сделано | Почему |
|---|---|
| Ревью веток 2b, 2c, 2d | Отдельная работа, не входит в очередь планов; долг остаётся записанным |
| Тринадцать находок ревью 2a | То же |
| Генерация `docs/rules.md` | План 5 |
| Регрессионные тесты из падений фаззера | Появятся вместе с первым падением; заводить пустой каталог незачем |
| Пер-файловые настройки в корпусе | Корпус проверяет `Default` в режиме HTML; остальные наборы — юнит-тесты правил |
